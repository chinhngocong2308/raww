var config = {
    paths: {
        slick: 'js/slick',
        carousel: 'js/owl.carousel.min'
    },
    shim: {
        slick: {
            deps: ['jquery']
        },
        carousel: {
            deps: ['jquery']
        }
    }
};